RewriteEngine On

# Proteger arquivos sensíveis
<Files ~ "\.(env|json|config|sql|log)$">
    Order allow,deny
    Deny from all
</Files>

# Prevenir acesso direto a diretórios
Options -Indexes

# Redirecionar para HTTPS (descomente quando tiver SSL)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# URL amigáveis (opcional)
# RewriteRule ^login$ login.php [L]
# RewriteRule ^caixa$ modules/caixa/index.php [L]